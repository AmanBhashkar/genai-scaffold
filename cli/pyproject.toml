[project]
name = "cli"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ChangeMe", email = "example@email.com" }
]
requires-python = ">=3.12"
dependencies = [
    "copier>=9.13.0",
    "jinja2>=3.1.6",
    "typer>=0.24.1",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
